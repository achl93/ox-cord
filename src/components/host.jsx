import React, { Component } from 'react'
import { getGeolocation } from '../getGeolocation'

export default class Host extends Component {
  render() {
    return (
      <div>
        Host a room! Login to Spotify.
      </div>
    )
  }
}